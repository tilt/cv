{
  "name": "till-breuer-cv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "sass src/styles/styles.scss assets/styles.css --style=expanded --no-source-map",
    "watch": "sass src/styles/styles.scss assets/styles.css --style=expanded --no-source-map --watch",
    "validate": "html-validate index.html"
  },
  "devDependencies": {
    "html-validate": "^9.7.1",
    "sass": "^1.101.0"
  }
}
